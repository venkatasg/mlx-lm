// Render as a fully static single-page app: no server-side rendering, and
// prerender the shell so `adapter-static` can emit a self-contained bundle.
export const prerender = true;
export const ssr = false;
